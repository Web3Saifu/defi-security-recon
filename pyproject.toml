[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "defi-security-recon"
version = "0.2.0"
description = "DeFiLlama Research: evidence-first DeFi protocol research and target-prioritization agent"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "DeFiLlama Research"}]
dependencies = []

[project.scripts]
defi-recon = "defi_recon.cli:main"
defillama-research = "defi_recon.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
